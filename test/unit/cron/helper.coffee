chai = require "chai"
assert = chai.assert

helper = require "../../../src/cron/helper"


describe "unit tests for cron helper", ->

  describe "function get_difference_in_hours", ->
    it "should return the search results", (done) ->
      #helper.get_difference_in_hours
      assert.ok 1 == 1
      done()

  describe "function object_to_array", ->
    it "should return the search results", (done) ->
      #helper.object_to_array
      assert.ok 1 == 1
      done()
